{
  "name": "zklogin-only-cross-institution-access",
  "version": "1.0.0",
  "description": "zkLogin end-to-end cross-institution access authorization & verification latency experiment (academic baseline for zkEHR evaluation)",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "experiment:prelogin": "./run-in-dcv.sh prelogin",
    "experiment:prime-consent": "./run-in-dcv.sh prime-consent",
    "experiment:prime": "./run-in-dcv.sh prime",
    "experiment:establish": "./run-in-dcv.sh establish",
    "experiment:cross-access": "./run-in-dcv.sh cross-access",
    "playwright:install": "playwright install --with-deps chromium"
  },
  "dependencies": {
    "@mysten/sui": "^1.45.2",
    "@noble/hashes": "^1.5.0",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "jwt-decode": "^4.0.0",
    "playwright": "^1.49.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^20.17.6",
    "typescript": "^5.6.3"
  }
}
